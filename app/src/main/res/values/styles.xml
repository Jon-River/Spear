<?xml version="1.0" encoding="utf-8"?>
<resources>

        <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
            <item name="android:progressDrawable">@drawable/rating_bar_full</item>
            <item name="android:minHeight">15dip</item>
            <item name="android:maxHeight">15dip</item>

        </style>

    <style name="MyActionBar.MenuTextStyle"
        parent="style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
    </style>
    <style name="TextAppearance.App.TextInputLayout" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="DialogAnimation" parent="@android:style/Theme.Dialog">
        <item name="android:windowAnimationStyle">@style/DialogAnimSettings</item>
    </style>

    <style name="DialogAnimSettings">
        <item name="android:windowEnterAnimation">@anim/slide_down</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

    <style name="MySwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/mustard</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/steelblue</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/Ash_Gray</item>
    </style>
    <style name="MyTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/Gunmetal</item>
        <item name="colorPrimaryDark">@color/Gunmetal</item>
        <item name="colorAccent">@color/mustard</item>
        <!-- Text input Edit Text Line color -->
        <item name="colorControlNormal">@color/greysnow</item>
        <!-- Text input Edit Text Hint color -->
        <item name="android:textColorHint">@color/greysnow</item>

        <!--To change the background of options menu-->
        <item name="android:itemBackground">@color/steelblue</item>
        <!--To change the text styling of options menu items</item>-->
        <item name="android:itemTextAppearance">@style/MyActionBar.MenuTextStyle</item>
        <!--<item name="android:windowActionModeOverlay">true</item>-->
    </style>


    <style name="TextAppearance.Collapsed" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>


    <style name="Expanded">

        <item name="android:textColor">@color/mustard85</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
